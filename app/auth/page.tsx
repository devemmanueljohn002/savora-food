"use client"; import {useState} from "react"; import PageShell from "@/components/PageShell";
export default function Page(){const [create,setCreate]=useState(false);return <PageShell><div className="auth"><div style={{textAlign:"center"}}><h1>Your Food. Your Choice. Delivered.</h1><p className="muted">Sign in or create an account to continue.</p></div><div className="form-card"><button className="btn secondary" style={{width:"100%"}}>Continue with Google</button><p style={{textAlign:"center"}} className="muted">— or use email —</p><label>I am signing up as</label><select><option>Customer</option><option>Vendor</option></select><div className="tabs"><span className={!create?"selected":""} onClick={()=>setCreate(false)}>Sign in</span><span className={create?"selected":""} onClick={()=>setCreate(true)}>Create account</span></div>{create&&<><label>Full name</label><input/></>}<label>Email</label><input/>{create&&<><label>Phone number</label><input placeholder="0803 000 0000"/></>}<label>Password</label><input type="password"/><button className="btn" style={{width:"100%",marginTop:10}}>{create?"Create account":"Sign in"}</button></div><p className="muted" style={{textAlign:"center"}}>♧ Your account, orders and bookings are stored securely in the cloud.</p></div></PageShell>}
